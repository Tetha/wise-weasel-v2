{
    "$defs": {
        "Act": {
            "properties": {
                "name": {
                    "title": "Name",
                    "type": "string"
                },
                "index": {
                    "title": "Index",
                    "type": "integer"
                },
                "puzzles": {
                    "additionalProperties": {
                        "$ref": "#/$defs/Puzzle"
                    },
                    "title": "Puzzles",
                    "type": "object"
                }
            },
            "required": [
                "name",
                "index"
            ],
            "title": "Act",
            "type": "object"
        },
        "Puzzle": {
            "description": "The display name of the puzzle.\n\nThis will be used e.g. as headers for the puzzle page, or as\nan identifier on the puzzle discovery page. This shuold make\na player recognize that this is the puzzle they are stuck on.",
            "properties": {
                "name": {
                    "title": "Name",
                    "type": "string"
                },
                "keywords": {
                    "items": {
                        "type": "string"
                    },
                    "title": "Keywords",
                    "type": "array",
                    "uniqueItems": true
                }
            },
            "required": [
                "name"
            ],
            "title": "Puzzle",
            "type": "object"
        }
    },
    "properties": {
        "name": {
            "title": "Name",
            "type": "string"
        },
        "acts": {
            "items": {
                "$ref": "#/$defs/Act"
            },
            "title": "Acts",
            "type": "array"
        }
    },
    "required": [
        "name"
    ],
    "title": "Game",
    "type": "object"
}